using WebAPIVenditaSolo.DataSource;

namespace WebAPIVenditaSolo.Logger
{
    public class Logger : ILogger
    {
        public void SalvaLogSuFB(string messaggio)
        {
            //using (LibreriaContext contesto = new LibreriaContext())
            //{
            //    contesto.Prodotti.Add(messaggio);
            //}
        }
    }
}
